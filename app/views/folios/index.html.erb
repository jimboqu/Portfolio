
<div class="album py-5 bg-light">
    <div class="container">
    	<%= link_to "New item", new_folio_path if logged_in?(:site_admin) %>
    	<div class="row <%= "sortable" if logged_in?(:site_admin) %>">
		<%= render partial: 'folio_item', collection: @folio_items %>
		</div>
	</div>
</div>

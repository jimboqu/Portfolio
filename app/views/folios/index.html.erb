<div class="album py-5 bg-light">
    <div class="container">
    	<div class="row">
		<%= link_to "Create new Folio item", new_folio_path if logged_in?(:site_admin)  %>
		<%= render partial: 'folio_item', collection: @folio_items %>
		</div>
	</div>
</div>

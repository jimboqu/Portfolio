<main role="main" class="container">
    <div class="row">
      <div class="col-md-8 blog-main">
      	<div class="blog-post">
			<h1><%= @topic.title %></h1>
			<%= paginate @blogs %>
     		<%= render @blogs %>
      		<%= paginate @blogs %>
		</div>
	  </div>
	</div>
</main>
